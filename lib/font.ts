import { Krona_One, Outfit } from "next/font/google";

const kronaOne = Krona_One({
  weight: "400",
  subsets: ["latin"],
});

const outfit = Outfit({
  weight: "600",
  subsets: ["latin"],
});

export { kronaOne, outfit };
